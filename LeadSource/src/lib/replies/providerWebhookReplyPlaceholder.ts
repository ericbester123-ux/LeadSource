export async function handleProviderWebhookReplyPlaceholder() {
  return {
    provider: "provider-webhook",
    enabled: false,
    message: "Provider webhook reply tracking is a placeholder for Stage 13. Manual reply tracking is active."
  };
}
